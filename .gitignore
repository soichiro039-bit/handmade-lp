# このリポジトリは中身がそのまま公開される。ここに入っている＝公開されている。
# まだ公開してはいけないものは、コミットせずこの無視リストで止める。
# このファイル自体も公開リポジトリから読めるので、経緯や社内の判断は書かないこと。

# 変更前の退避ファイルを同じ場所へ置かない（置くと是正前の内容が公開URLで読める）。
# 万一置かれた時の保険として無視する。退避先はこのリポジトリの外にすること。
*_backup_*.html
*.bak
*.bak_*
